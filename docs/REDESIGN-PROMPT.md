# פרומפט שדרוג עיצוב — Inventory Planner

> **למי זה מיועד:** Claude Code שרץ על המאגר `yairhaygur-sketch/inventory-planner`.
> **מה זה:** מפרט עיצוב מלא + תוכנית ביצוע בשלבים. כל שלב נבנה, נבדק ונדחף בנפרד.
> **אילוסטרציות — מקור האמת החזותי:** https://claude.ai/code/artifact/e59db8da-8647-42c8-a6e8-9fcabefec0bc
> שלושת המסכים שם בנויים ב-HTML/CSS אמיתי עם הטוקנים של המפרט. כשיש סתירה בין הטקסט לאילוסטרציה — האילוסטרציה קובעת.

---

## 0 · לפני שאתה נוגע במשהו — קרא את זה

הכלי הזה אינו אתר. הוא **כלי עבודה שמופעל פעמיים ביום** על ידי מתכנן אחד,
שמסתכל על 7,569 פריטים כדי להחליט מה לעשות בשעה הקרובה.

כל החלטת עיצוב נמדדת בשאלה אחת: **האם היא מקצרת את הדרך מ-7,569 שורות
להחלטה אחת?** אם התשובה לא — היא לא נכנסת, גם אם היא יפה.

### חמישה אילוצים קשיחים

1. **קובץ יחיד.** `index.html` בלבד. אין build, אין bundler, אין קבצי CSS/JS נפרדים.
2. **RTL עברית.** כל מאפייני המיקום הם לוגיים (`inset-inline-start`, `padding-inline-end`,
   `text-align:start`). `left`/`right` פיזיים הם באג. זה כבר גרם לתקלת יישור בכל האתר פעם אחת.
3. **383 בדיקות חייבות לעבור.** הן נשענות על מבנה ה-DOM: `#tbl tbody tr[data-i]`,
   `.wdetail`, `.wqueue`, `#kpis`, `.grp`, `body.dopen`, `body.push`, `body.dark`.
   **אל תשנה סלקטורים שהבדיקות משתמשות בהם** — הוסף לצידם. אם אתה חייב לשנות,
   עדכן את הבדיקה באותו קומיט והסבר למה.
4. **CDN מוגבל.** SheetJS מ-`cdn.sheetjs.com`, גופנים מ-`fonts.googleapis.com`. אל תוסיף מקורות.
5. **לא לגעת בלוגיקה העסקית.** `build()`, `classify()`, `analyze()`, כל הקבועים
   (`DEAD_DAYS`, `LOWDEM_CHEAP`, `MARK_TTL`, `TREND_*`) — מחוץ לתחום. זה שינוי עיצוב.

### מה לא לעשות — נלמד מהתמונה שהמתכנן צירף כהשראה

ההשראה היא תבנית מעקב-משימות עם 24 שורות ולוח קנבן של כרטיסים.
**אל תבנה קנבן.** 2,711 פריטי עודף בכרטיסים הם 2,711 גלילות. מה שכן נלקח משם:
צבע שנושא משמעות, שורת מדדים כמסך פתיחה, והיררכיית טיפוגרפיה חדה.

---

## 1 · החוב שצריך לשלם קודם — מערכת הצבעים מוגדרת ארבע פעמים

זו לא הערת סגנון. זה באג מבני שחוסם כל שינוי עיצוב.

```
שורה  11  :root{ --acc:#5b5bd6; --r:14px; --shadow:0 1px 2px…; … }
שורה  19  body.dark{ … }
שורה 304  :root{ --acc:#3f77ad; --r:0;  --shadow:none; … }   ← זה מה שמנצח
שורה 312  body.dark{ … }
שורה 387  :root{ --panel0; --soft }
שורה 803  :root{ --f-xs … --f-xl }
```

הבלוק בשורה 11 **מת כמעט כולו** — הקסקדה דורסת אותו. מי שיפתח את הקובץ
וישנה את `--acc` שם לא יראה שום דבר קורה, ויחשוב שהוא משוגע.

### וזה לא רק `:root` — ל-`.tab` יש בדיוק אותה מחלה

```
47 בלוקי כללים נוגעים ב-.tab, בשבעה הקשרים שונים:
שורה 121  .tab{border-radius:10px;min-width:88px}      ← הבסיס
שורה 269  .wqueue .tabs{grid} .tab{border-radius:6px}  ← דורס
שורה 357  .tab{border:0;border-top:4px solid}          ← דורס שוב
שורה 416  .tab{flex:1 1 0;min-width:132px}             ← ושוב
שורות 480,482,643,648,704,762,777,780,786              ← ועוד תשעה
```

`.tab` משמש גם לכפתורי המצב וגם לטאבים של פאנל התנועות (`#movtabs`).
**שלב 3 הוא השלב היקר, לא שלב 0.** מי שיתחיל לכתוב סרגל טאבים חדש
בלי לפרק את זה קודם — ייצור בלוק 48.

**המלצה:** תן לסרגל החדש מחלקה משלו (`.navtab`), אל תיגע ב-`.tab` הקיים,
ומחק אותו רק אחרי שהחדש עובד ו-`#movtabs` הועבר גם הוא.

**שלב 0 — ניקוי, לפני כל שינוי חזותי:**

- אחד את כל ארבעת הבלוקים לבלוק טוקנים **אחד** בראש ה-`<style>`
- כל טוקן מוגדר **פעם אחת** ב-`:root`; `body.dark` מגדיר מחדש **רק ערכים**, לא טוקנים חדשים
- מחק כל טוקן שאף אחד לא משתמש בו (בדוק ב-grep לפני מחיקה)
- **אפס שינוי חזותי בשלב הזה.** צלם לפני ואחרי וודא שהם זהים

בלי זה, כל שלב אחר נבנה על חול.

---

## 2 · מערכת הצבע החדשה

### העיקרון: צבע הוא מידע, לא קישוט

היום אדום מופיע בכפתור מצב «היום», ב-`rowColor`, ב-`sCatStyle` וב-KPI.
מכאן והלאה:

> **אדום שמור לחומרה 3 בלבד.** אם משהו אדום — מישהו ממתין עכשיו.
> כפתורי ניווט, כותרות וקישוטים לא נוגעים באדום.

### הפלטה

בהיר הוא **ברירת המחדל** מעכשיו. כהה נשאר, שווה-ערך, לא נספח.

```css
:root{
  /* משטחים — נייר קריר עם הטיה כחולה, לא לבן טהור */
  --paper:      #F2F5F8;   /* רקע העמוד */
  --surface:    #FFFFFF;   /* כרטיסים, טבלה */
  --surface-2:  #E9EEF4;   /* שקוע: כותרות טבלה, מגירה */
  --line:       #D8E0E9;
  --line-soft:  #EAEFF4;

  /* דיו */
  --ink:        #151A21;   /* טקסט ראשי */
  --ink-2:      #5A6673;   /* משני */
  --ink-3:      #636D7A;   /* שלישוני, תוויות */

  /* מבטא — כחול פלדה תעשייתי. זה צבע ה"מערכת", לא צבע התרעה */
  --accent:     #1F5C8B;
  --accent-ink: #0F3A5A;
  --accent-bg:  #E6EEF5;

  /* סמנטיקה — חמישה תפקידים, חמישה צבעים, בלי חפיפה */
  --critical:   #C0392B;   /* חומרה 3 — לקוח ממתין, מלאי אפס */
  --critical-bg:#FBECEA;
  --high:       #AA5D05;   /* חומרה 2 — אוזל החודש, מניעת חוסר */
  --high-bg:    #FEF4E7;
  --steady:     #1F5C8B;   /* חומרה 1 — פרמטרים, מעקב. זהה למבטא במכוון */
  --steady-bg:  #E6EEF5;
  --capital:    #2D7968;   /* הון כלוא — הזדמנות, לא סכנה */
  --capital-bg: #E7F2EF;
  --calm:       #656F7C;   /* חומרה 0 — רקע, תקין */
  --calm-bg:    #EFF2F5;
}
```

**ההחלטה החשובה כאן: `--capital` בטורקיז עמוק.**
היום ההון הכלוא — **$1,046,437 + ₪227,424 + €1,701**, המספר הגדול ביותר בכלי —
מוצג באפור ניטרלי. הוא לא סכנה ולא תקלה; הוא **כסף שאפשר לשחרר**.
מגיע לו צבע משלו, והוא לא אדום ולא אפור.

### כהה — הגדרת ערכים בלבד

```css
body.dark{
  --paper:#0E1217; --surface:#161B22; --surface-2:#1D242D;
  --line:#2A3340; --line-soft:#212932;
  --ink:#E6EBF1; --ink-2:#98A3B0; --ink-3:#808B97;
  --accent:#5B9BD1; --accent-ink:#8FBCE3; --accent-bg:#16283A;
  --critical:#E06A5E; --critical-bg:#2E1614;
  --high:#E0A055;   --high-bg:#2F2213;
  --steady:#5B9BD1; --steady-bg:#16283A;
  --capital:#4FA88F; --capital-bg:#122823;
  --calm:#8A94A0;   --calm-bg:#1D242D;
}
```

### מילוי כהה — חריג אחד שחייב כלל משלו

`#FFFFFF` על `--accent` הכהה (`#5B9BD1`) נותן **2.98:1 — נכשל.**
במצב כהה, טאב פעיל וכפתור ראשי מקבלים **דיו כהה על המילוי הבהיר**, לא לבן:

```css
body.dark .tab.on, body.dark .btn-primary{ color:var(--paper) }   /* 6.31:1 */
```

זה הפוך ממצב בהיר, וזה מכוון. אל "תתקן" את זה ללבן.

**בדיקת קבלה:** כל צמד טקסט/רקע עובר יחס ניגודיות 4.5:1 בשני המצבים.
הרץ בדיקה שמחשבת את זה על כל הצמדים ומפילה את הבנייה אם אחד נופל.

> **הערכים לעיל נמדדו, לא נבחרו לפי תחושה.** הטיוטה הראשונה של הפלטה
> נכשלה בשש צמדים במצב בהיר ובשניים בכהה. `--high` היה `#D97706` (2.93:1),
> `--ink-3` היה `#8A94A0` (3.08:1), `--capital` `#2E7D6B` (4.30:1),
> `--calm` `#6B7683` (4.11:1). הערכים הנוכחיים הם הגוון הקרוב ביותר
> לכוונה המקורית שעדיין עובר 4.5:1 מול **כל** הרקעים שהוא מופיע עליהם.
> אם אתה משנה ערך — הרץ את הבדיקה מחדש. אל תנחש.

---

## 3 · טיפוגרפיה

היום: `--f-xs:10px … --f-xl:24px`, גוף 13px, `Assistant` + `Archivo` למספרים.

הבסיס נכון. מה שחסר הוא **היררכיה** — כל דבר נראה חשוב באותה מידה.

```css
:root{
  --f-micro: 10.5px;  /* תוויות uppercase, letter-spacing .04em */
  --f-sm:    12px;    /* משני, עזר */
  --f-base:  13.5px;  /* גוף, תאי טבלה */
  --f-lg:    16px;    /* כותרות קטעים */
  --f-xl:    22px;    /* כותרת מסך */
  --f-num:   28px;    /* מספר במדד */
  --f-hero:  40px;    /* המספר האחד במסך הפתיחה */
}
```

- **מספרים תמיד `font-variant-numeric: tabular-nums`** ובגופן `--num`. כבר קיים — החל בכל מקום.
- **כותרות `text-wrap: balance`.**
- **תוויות באותיות גדולות** מקבלות `letter-spacing: .04em` ו-`--ink-3`.
- **משקל, לא גודל, יוצר היררכיה בטבלה צפופה.** 13.5px/700 מול 13.5px/400 קורא
  טוב יותר מ-16px מול 13px, ולא מרחיב את השורה.

---

## 3ב · מערכת הכפתורים

**המתכנן הצביע על התמונה השנייה ואמר «ככה אני רוצה».** זה סעיף מחייב.

הכלל שמחזיק את כולם: **מה שנראה לחיץ — לחיץ, ומה שלא — לא.**
היום שורת המדדים נראית כמו כרטיסים ואי אפשר ללחוץ עליה.

רדיוס חדש **לפקדים בלבד**: `--r-ctl: 6px`.
`--r: 0` נשאר לכרטיסים, לטבלה ולפאנלים. זו לא אי-עקביות — זו ההבחנה
בין מה שלוחצים לבין מה שקוראים.

### 1 · טאב ניווט — מחליף את כפתורי המצב ואת המסלולים הנסתרים

שבעת המשטחים בשורה אחת: היום · החודש · הקטלוג · רצפת SS · מגמה ·
לקוח ממתין · יישום. כל אחד: **אייקון 15px + תווית + תג מספר**.

- **פעיל:** ממולא ב-`--accent`, טקסט ואייקון לבנים
- **כבוי:** רקע שקוף, טקסט `--ink-2`, אייקון בצבע הסמנטי שלו
- **תג המספר:** `--calm-bg`. כשיש בו חומרה 3 — `--critical-bg` ואדום,
  **גם כשהטאב לא פעיל**. ככה «היום · 48» צועק כשיש 28 בוערים.

### 2 · כפתור מדד — השדרוג התפקודי היחיד במסמך

בתמונה, שלושת החסומים הצבעוניים ב-Risk Tracker הם **כפתורים**.
אצלנו `#kpis` הוא טקסט מת.

כל מדד הופך לכפתור: נקודה צבעונית + מספר גדול + תווית, על רקע סמנטי בהיר.
**לחיצה מסננת את הטבלה** לאותה קבוצה; הנבחר מקבל טבעת `--accent`.
מדד ההון מציג את המטבע הראשי בגדול ואת השאר בתווית — בלי לחבר.

### 3 · צ'יפ סינון נדלק

`✓` כשדלוק, בצבע הסמנטי של מה שהוא מסנן. כבוי — מסגרת דקה בלבד.
עגול מלא (`border-radius: 99px`), בניגוד לטאבים.

### 4 · פעולה

ראשי ממולא ב-`--accent`; משני שקוף עם מסגרת; `sm` לסרגל הסינון.

### 5 · כפתור אייקון בפינת פאנל

24×24, שקוף עד ריחוף. קיפול · עמודות · ייצוא · סגירה.
בפינת כל פאנל, כמו ה-`≡` וה-`⚙` בתמונה.

---

## 4 · שלושת המסכים

### 4א · מסך הטעינה — «תדריך פתיחה»

**זה השינוי שהמתכנן ביקש במפורש, ולדעתי הוא בעל הערך הגבוה ביותר בכל המסמך.**

היום: מסך ריק עם כפתור קובץ. אחרי טעינה — קופצים ישר לטבלה של 48 שורות.
המתכנן לא יודע מה השתנה, מה דחוף, ואם מה שתיקן בפעם הקודמת תפס.

**המסך החדש, שלושה מצבים:**

**(1) לפני — אזור שחרור**

אזור גדול, מקווקו, `--accent` בריחוף. טקסט שאומר מה הכלי מצפה לו:
«דוח ZMRP מ-SAP · 114 עמודות · גרור לכאן או בחר קובץ».
מתחת, בקטן: מאיפה מוציאים אותו.

**(2) בזמן הטעינה — שלבים אמיתיים, לא ספינר**

הכלי כבר יודע כמה זמן כל שלב לוקח. הצג את זה:

```
פענוח הקובץ        ████████████░░░░  3.2 שנ׳
בניית הפריטים      ░░░░░░░░░░░░░░░░   91 מ״ש
סיווג              ░░░░░░░░░░░░░░░░   15 מ״ש
רינדור             ░░░░░░░░░░░░░░░░   31 מ״ש
```

ספינר אומר «חכה». שלבים אומרים «אני עובד, וזה מה שנשאר».

**(3) אחרי — התדריך**

**לא** לקפוץ לטבלה. להציג מסך אחד שעונה על ארבע שאלות, ואז כפתור «התחל לעבוד».

| הכרטיס | מה בפנים | מקור הנתון |
|---|---|---|
| **הדוח** | 7,569 פריטים · נטען ב-3.4 שנ׳ · חודש בסיס | קיים |
| **בוער עכשיו** | 3 לקוחות ממתינים ללא רכש · 28 פריטים בחומרה 3 | `Q.waiting`, `sev===3` |
| **הון לשחרור** | $1,046,437 · ₪227,424 · €1,701 — **כל מטבע בשורה נפרדת** | `Q.excess` |
| **מה השתנה** | פריטים חדשים בתור · חוסרים שהחמירו · פריטים שיצאו | `planner_history_v1` |
| **מה יושם** | מתוך N המלצות מההרצה הקודמת — X בוצעו, Y שונו אחרת, Z לא נגעו | `planner_params_v1` |

שני הכרטיסים האחרונים הם **המידע היחיד בכלי שלא זמין בשום מקום אחר**,
והם קבורים היום במסלול נסתר. בקצב של שני דוחות ביום הם הדבר הראשון
שהמתכנן צריך לראות.

**אזהרות DIAG** מופיעות כאן כפס צהוב מעל הכרטיסים, לא כמודאל שצריך לסגור.

**קריטי:** אל תשבור את המסלול הקיים. הבדיקות טוענות קובץ ומצפות שהטבלה
תהיה שם. הוסף `body.briefing` שמציג את התדריך, וכפתור «התחל לעבוד» שמסיר
אותו. בסביבת בדיקה — או שהבדיקות לוחצות על הכפתור, או ש-`briefing` מדולג
כשיש פרמטר. **בחר אחת והצהר עליה בקומיט.**

---

### 4ב · מסך העבודה

היום: שורת כפתורי מצב + ארבעה ווידג'טים נגררים. הבעיה — **המסלולים הנסתרים**
(רצפת SS, מגמה, לקוח ממתין, יישום) מופיעים ככפתורים שצצים ונעלמים לפי מצב.
הם קיימים, הם שווים כסף, ואי אפשר למצוא אותם.

**סרגל הטאבים** מחליף גם את כפתורי המצב וגם את המסלולים הנסתרים —
שבעה טאבים בשורה אחת, כל אחד עם מספר. ראה סעיף 3ב.

**מה ויתרנו עליו ולמה זה בסדר.** גרסה קודמת של המפרט הציעה סרגל צד,
שיכול היה לשאת תחת «רצפת SS» גם את הסכום `$127,834 · ₪106,574`.
טאב לא יכול. **הסכום עובר לכפתורי המדד** ברגע שנכנסים למסלול — וזה
המקום הנכון שלו ממילא: המספר עוזר להחליט מה לעשות *בתוך* המסלול,
לא אם להיכנס אליו. מה שצריך כדי לבחור מסלול הוא הספירה, והיא בתג.

**הטבלה:**

- **פס חומרה** ברוחב 3px בקצה ההתחלה של השורה, בצבע הסמנטי. מחליף את
  צביעת הטקסט הנוכחית (`rowColor`) — טקסט צבעוני פוגע בקריאות של 48 שורות.
- **שורת קבוצה** (`tr.grp`) מקבלת רקע `--surface-2` ומשקל 700. היום היא כמעט נעלמת.
- **כל המספרים** — `tabular-nums`, יישור `start`, גופן `--num`.
- **ריחוף** מרים את השורה ב-`--surface` ומראה את פעולות השורה. אל תשנה
  `tbody tr:hover` — הרחב אותו.
- **בלוקי מטבע** במסלול ההון הכלוא: כותרת מטבע דביקה עם הסכום שלו.

**שורת המדדים (`#kpis`)** — היום מוסתרת כברירת מחדל. **החזר אותה**,
כשורת כפתורי מדד לפי סעיף 3ב. היא התשובה ל«מה המצב», היא כרגע כבויה,
וכשהיא תידלק היא גם תסנן.

---

### 4ג · המגירה

היום המגירה דוחפת את הטבלה (נבנה ב-PR #38) והתוכן שלה עמוס: הסבר, החלטות,
היסטוריה, פרמטרים, גרף — הכול בזרם אחד.

**החדש — תיק פריט:**

- **כותרת דביקה:** מק"ט · תיאור · צ'יפ חומרה · כפתור סגירה
- **שורת החלטה** מיד מתחת, ב-`--surface-2`: מה לעשות, בגדול. זו הסיבה
  שהמגירה נפתחה.
- **שלושה טאבים:** `החלטה` · `נתונים` · `היסטוריה`
  - החלטה — `why` + `act` כרשימה ממוספרת, לא כפסקה
  - נתונים — פרמטרים לפני→אחרי, גרף 11 החודשים, מלאי/רכש/לקוח
  - היסטוריה — הרצות קודמות, סימונים, מה יושם
- **הגרף** מקבל יחס מלא: מילוי שטח, רשת עדינה, נקודת סיום מודגשת,
  חודשי אזילה ופיקים מסומנים. היום הוא sparkline זעיר.

---

## 5 · תנועה

מעט, ומכוונת. הכלי נפתח פעמיים ביום — אנימציה שמרשימה בפעם הראשונה
מעצבנת בפעם החמישים.

| איפה | מה | כמה |
|---|---|---|
| פתיחת מגירה | החלקה + דחיפת הטבלה | 160ms `ease-out` — **קיים, אל תיגע** |
| מעבר מצב | הצלבה של תוכן הטבלה | 120ms |
| שלבי הטעינה | מילוי הפס | זמן אמת |
| ריחוף שורה | רקע בלבד | 80ms |
| כל השאר | — | אין |

`@media (prefers-reduced-motion: reduce)` מבטל הכול חוץ ממילוי הפס.

---

## 5ב · ארבע ההחלטות של המתכנן — 14.9.2026

הוצגו לו ארבע שאלות עם המחיר של כל תשובה. אלה התשובות. **הן מחייבות.**

| # | השאלה | ההחלטה | מה זה אומר בקוד |
|---|---|---|---|
| 1 | קצב מיזוג — הכלי מתפרסם מ-`main`, כל מיזוג עולה חי | **שלבים 0–2 מתמזגים אחד אחד. שלבים 3–5 יושבים על הענף עד אישור אחד.** | 0–2 הם שינוי חזותי הפיך ב-`git revert`. 3–5 משנים ניווט; חצי-מחווט הוא כלי שבור. |
| 2 | מתי מסך התדריך מופיע | **רק כשיש משהו חדש.** | צריך להגדיר «חדש». ראה למטה. |
| 3 | הסכום שיושב היום על `#floorBtn` | **עובר לכפתורי המדד.** | הטאב נושא ספירה; `floorV`/`custV`/`trendV` עוברים לשורת המדדים בתוך המסלול. |
| 4 | `rowColor` — טקסט צבעוני | **פס 3px בקצה, טקסט רגיל.** | `rowColor` הופך למקור הצבע של הפס במקום של הטקסט. |

### מה נחשב «חדש» לצורך החלטה 2

המסך מופיע אם **לפחות אחד** מאלה נכון מול ההרצה הקודמת ב-`planner_history_v1`:

1. פריט חדש נכנס ל-`Q.waiting` או קיבל `sev===3`
2. מספר הפריטים ב-`Q.immediate` גדל
3. יש אזהרת `DIAG` פעילה
4. עברו יותר מ-24 שעות מההרצה הקודמת

אחרת — ישר לטבלה, עם פס דק אחד למעלה שאומר «אין שינוי מאז ההרצה הקודמת ·
הצג תדריך» כקישור. **המידע לא נעלם, הוא רק לא חוסם.**

**שים לב לכשל האפשרי:** אם `planner_history_v1` ריק או פגום, אין עם מה
להשוות. במקרה כזה **הצג את התדריך** — עדיף להראות מיותר מאשר להחביא שינוי.

---

## 6 · תוכנית הביצוע — שישה שלבים, כל אחד PR בפני עצמו

**אל תעשה הכול בקומיט אחד.** הכלי חי, והמתכנן עובד עליו פעמיים ביום.

| # | שלב | מה נכנס | קבלה |
|---|---|---|---|
| **0** | איחוד טוקנים | ארבעת בלוקי ה-`:root` לאחד | 383 עוברות · **אפס שינוי חזותי** |
| **1** | פלטה + טיפוגרפיה | הצבעים החדשים, בהיר כברירת מחדל, סולם הטיפוגרפיה, `--r-ctl` | 383 עוברות · ניגודיות 4.5:1 בשני המצבים |
| **2** | טבלה | פס חומרה, שורות קבוצה, מספרים טבלאיים, ריחוף | 383 עוברות · אין גלישה אופקית |
| **3** | כפתורים וניווט | סרגל טאבים לשבעת המשטחים, כפתורי מדד שמסננים, צ'יפים, כפתורי אייקון | הבדיקות שמחפשות כפתורי מצב מעודכנות באותו קומיט |
| **4** | מסך הטעינה | שלושת המצבים + חמשת כרטיסי התדריך | בדיקה חדשה: התדריך מציג את המספרים הנכונים מהדוח האמיתי |
| **5** | מגירה | תיק פריט עם טאבים, גרף מלא | `body.push` ממשיך לעבוד · בדיקות המגירה עוברות |

**אחרי כל שלב:** `npm test` מקומית, צילום מסך של הדוח האמיתי, PR עם
לפני/אחרי. אל תתחיל שלב לפני שקודמו מוזג.

---

## 7 · איך למדוד שזה הצליח

לא «נראה יותר יפה». מדדים:

1. **מהמסך הראשון להחלטה הראשונה** — כמה לחיצות עד שהמתכנן יודע מה לעשות?
   היום: טען → המתן → סרוק 48 שורות → לחץ. מטרה: התדריך עונה לפני הלחיצה הראשונה.
2. **המסלולים הנסתרים נמצאים** — רצפת SS מחזיקה $127,834 + ₪106,574 ויושבת
   מאחורי כפתור שנראה רק בשני מצבים מתוך שבעה. אחרי השינוי הוא טאב קבוע עם ספירה.
3. **ניגודיות** — כל צמד עובר 4.5:1. נמדד, לא נאמד.
4. **383 בדיקות** — עוברות בכל שלב. לא «נתקן אחר כך».

---

## 8 · מה לא נכנס, ולמה

| רעיון | למה לא |
|---|---|
| לוח קנבן לפריטים | 2,711 פריטי עודף. קנבן הוא ממשק לעשרות, לא לאלפים. |
| סרגל צד | נשקל ונדחה. המתכנן הצביע על סרגל טאבים ואמר «ככה אני רוצה», והוא גם קרוב יותר למה שכבר קיים. |
| גרדיאנטים, זכוכית, נאון | הכלי נפתח פעמיים ביום מול טבלאות. אפקטים שמרשימים פעם אחת מפריעים בפעם החמישים. |
| אייקונים כמחליפי טקסט | עברית + מונחי SAP. אייקון ליד טקסט — כן. במקומו — לא. |
| שינוי כל הרדיוסים ל-12px | `--r:0` היום זו בחירה עקבית, לא שכחה. הרדיוס עולה רק לכרטיסי התדריך והמגירה. |

---

## 9 · המשפט האחד

> הכלי הזה הופך 7,569 שורות אקסל להחלטה אחת.
> העיצוב קיים כדי לקצר את הדרך הזאת — לא כדי לקשט אותה.
