# שלב ג׳ — בניית הממשק מחדש

ענף: `claude/ui-redesign` · בסיס: `main` @ `35a47b8`
המסמך מתעדכן בכל סבב. הוא אינו החלטה עסקית — ההחלטות יושבות ב-
`BUSINESS-DECISIONS.md` ו-`DECISION-RULES.md` ולא זזו.

---

## 1. הוכחה שהלוגיקה העסקית לא השתנתה

אותו קובץ (`tests/zmrp-demo.xlsx` + `tests/zmrp-demo-eta.xlsx`), אותם
פרמטרים, שתי גרסאות: `main` מול הענף. נמדד ישירות מתוך `ALL` אחרי
`build()` ו-`classify()`.

**חתימה לכל פריט** — מק״ט · סיווג · חומרה · חוסר · כמות הזמנה · SS מוצע ·
ROP מוצע · בוער · תיקון פרמטרים · הון כלוא · שווי החוסר · כמות עודף ·
מקור ה-SS:

```
900 פריטים · 900 חתימות · 0 הבדלים
```

**מצרפים:**

| מדד | הערך | זהה ל-main |
|---|---|---|
| פריטים | 900 | ✔ |
| סה״כ חוסר (יח׳) | 3,040 | ✔ |
| סה״כ כמות הזמנה מומלצת | 222 | ✔ |
| סה״כ SS מוצע | 3,100 | ✔ |
| סה״כ ROP מוצע | 11,587 | ✔ |
| לקוח ממתין ללא רכש | 16 | ✔ |
| תיקוני פרמטרים | 49 | ✔ |
| הון כלוא | $62,410,622 · €22,374,734 · ₪14,610,282 | ✔ |
| שווי החוסר | $2,157,845 · €660,356 · ₪424,412 | ✔ |

**סיווגים** (12 ערכים) — זהים אחד לאחד:
רקע 485 · פער כיסוי להזמנת לקוח 118 · עודף מלאי 82 · אוזל החודש 45 ·
מלאי מת 43 · עדכון פרמטרים 36 · לפי דרישה 24 · חשד אזילה 22 ·
לקוח ממתין – אין רכש 16 · מלאי איטי 15 · מניעת חוסר 9 · מעקב אספקה 5.

**אורכי הרשימות** (המקור שכל מסך שואב ממנו) — זהים:
בוער 16 · לקוח ממתין 134 · רצפת SS 136 · מגמה יורדת 115 ·
ביקוש עולה 94 · הון כלוא 311.

### ההבדל היחיד שנמצא, והוא תיקון

שלושה מונים בסרגל הניווט (`navCount`) השתנו — ולא הרשימות עצמן:

| מונה | main | הענף | למה |
|---|---|---|---|
| `done` | 900 | 0 | `modeRows` נופל לכל הפריטים כש-`cur` אינו `'today'`. הדלת הציגה 900 במקום מספר הסימונים. |
| `rise` | 900 | 94 | אותו כשל. 94 הוא אורך `riseRows()` — הרשימה שהמסך עצמו מציג. |
| `cap` | 0 | 311 | «בריאות המלאי» לא היה תחום ב-main. 311 הוא אורך `decisionList('cap')`. |

בכל שלושת המקרים המונה החדש שווה בדיוק לאורך הרשימה שהוא מצביע אליה.
שום פריט לא נכנס או יצא מרשימה כלשהי.

---

## 2. מה נבנה

### המסילה — שישה תחומי עבודה

`מרכז עבודה · חוסרים ולקוחות · רכש ואספקות · תכנון ו-MRP · בריאות
המלאי · קטלוג פריטים`, במסילה אנכית קבועה בצד.

* תחום אינו נעלם כשהמונה אפס — הוא מוצג עם תג כבוי.
* כל מסלול ותיק נשאר נגיש כדלת משנה בתוך התחום שלו:
  `burn · cust · done` בחוסרים · `line · rise` באספקות ·
  `month · floor · trend · applied` בתכנון · `catalog · moves` בקטלוג.
  14 יעדים נגישים, אפס אבדו.
* שם אחד לכל מסך: הכותרת נגזרת מאותו מקור שהמסילה מציגה.

### מרכז העבודה

מסך נחיתה שמצביע על ארבע רשימות קיימות (שבע שורות ראשונות בכל אחת)
וקישור לרשימה המלאה. פערי הנתונים נאמרים מעליהן.

### כרטיס הפריט

רצועת מספרים בראש ההחלטה — `לקוח · פנוי · רכש פתוח · בהעברה · פער`,
כל אחד שדה מנוע כפי שהוא. `בהעברה` אומר «לא ידוע» כשאין עמודת CK.
‹ › ומיקום ברשימה, על אותה `moveSel` שהמקלדת מפעילה.

### צפיפות

«רגילה» ו«צפופה». הצפופה מורידה ריפוד בלבד — הגופן אינו קטן.
שורה 33px → 26px · שורות שנכנסו 15 → 18 (1440×860).

---

## 3. מה נמדד

| מדד (1440×860, אותו דוח) | לפני | אחרי |
|---|---|---|
| כרום מעל הרשימה | 339px | 213px |
| שורות גלויות | 15 | 18 (22 בצפופה על מסך 1080) |
| רצועת הדלתות בשורת הכותרת | 690px מתוך 1008 | 0 |
| מסילת הסינון כשהיא פתוחה | 683px גובה | 81px |
| גלישה אופקית (1180–1920) | ב-1366 וב-1440 | אין |
| ניגודיות — כשלים | — | 0 בשישה מעברים (3 מסכים × 2 מצבים) |
| בדיקות | 860 | 882 |

---

## 4. תקלות שנמדדו ותוקנו במהלך הבנייה

1. `navCount` החזיר 900 ל«טופלו» ול«ביקוש עולה» (ראה סעיף 1).
2. ספי הצפיפות של הטבלה היו לפי רוחב חלון; המסילה לוקחת 164–224px,
   ולכן הטבלה גלשה ב-1366 וב-1440. הספים הוזזו ברוחב המסילה.
3. ריפוד כפול דחף את הטבלה אל מתחת למגירה — 42px חפיפה ב-1920.
4. רקע המגירה נדרס לשקוף; שורות הטבלה נראו מבעד לכרטיס הפריט.
5. סמלי התחומים וצבע הבחירה נתנו 1.71–2.77 ניגודיות על רקע המסילה.
6. מסילת הסינון נפרסה כרצועה בגובה 683px מתוך 804 — `.rows` נשאר עם
   0px והשורה הראשונה ירדה מתחת ל-`.pager`.
7. כותרת מסילת הסינון נחסמה ב-`display:none` גורף, ואיתה ה-✕ המקפל.
8. `→` בזוג ROP נקרא הפוך בכיוון RTL («121 → 22» על העלאה מ-22 ל-121).

---

## 4ב. הסבב השני — עמודות, מיון, ייצוא, בריאות המלאי

ענף `claude/ui-columns`, מעל `main` שאחרי המיזוג.

### התקלה שהולידה את רישום העמודות

נמדד על main, בקיבוץ «לפי ספק» בתור העבודה: **12 כותרות מול 11
תאים**, וכל עמודה אחרי «תיאור» נשאה כותרת של עמודה אחרת. מי שקרא
«מלאי 39» קרא בפועל את דרישת הלקוח; «שווי» הראה את הוותק. המספרים
היו נכונים, השמות מעליהם לא.

הסיבה מבנית: הכותרות נבנו בפונקציה אחת והתאים בשתי תבניות אחרות.
שלוש רשימות נפרדות אינן נשארות מסונכרנות ידנית.

**מה נבנה:** רישום עמודות (`SCOL`) שבו לכל עמודה הגדרה אחת — שם,
הסבר ותא — והכותרת והשורה נגזרות ממנה. אי-התאמה אינה אפשרית, ובדיקה
נועלת «כותרת לכל תא» בשתי התצוגות ובכל מצב של הבורר. ברירות המחדל
לא השתנו: הרישום החליף את מקור האמת, לא את מה שרואים.

### בורר עמודות

19 עמודות זמינות. הבחירה נשמרת לכל תצוגה בנפרד ושורדת רענון.
«מק״ט» אינו ניתן להסרה. עמודה שאינה אפשרית במצב הנתונים — «מכוסה»
בלי דוח ETA, «בדרך ⌛» איתו — מוצגת כבויה עם הסיבה.

שלוש עמודות שהמפרט ביקש נוספו: **רכש פתוח** (הסכום הגולמי מול
הספק, לפני הפיצול לפי תאריך), **בהעברה** (עמודת CK; «לא ידוע»
כשאין עמודה כזו — לא 0), ו**הגעה**.

### מיון בתוך הקיבוץ

לטבלת העבודה לא היה מיון כלל. עכשיו לחיצה על כותרת ממיינת יורד,
שנייה עולה, שלישית מחזירה לסדר המקורי. המיון פועל בתוך כל קבוצה
ובתוך כל בלוק מטבע. נמדד בגרסה הראשונה של התיקון: מיון שזלג בין
המטבעות פיצל את כותרות המטבע מ-8 ל-75 — כלומר הפך «דירוג בתוך
המטבע בלבד» לרשימה מעורבת.

### הקפאת מק״ט ותיאור

נדלקת רק כשיש גלילה אופקית בפועל, וההיסט נמדד מהתאים ולא מקובע.

### הייצוא מייצג את מה שרואים

השורות תמיד היו של התצוגה, אבל העמודות היו 51 קבועות והסדר היה של
`currentRows()` ולא של המסך. עכשיו הגיליון הראשון הוא «התצוגה» —
העמודות הגלויות, בסדר שלהן, ובסדר השורות שעל המסך. הגיליון המלא
נשאר אחריו.

### בריאות המלאי

התחום היה במסילה עם מונה נכון, אבל הגוף שלו היה בדיוק הקטלוג
(`cur:'all'`, 900 פריטים, עמודת «הון כלוא» ריקה ברוב השורות).
עכשיו הטבלה היא `decisionList('cap')` — 311 הפריטים שיש להם הון
כלוא, בבלוקי מטבע עם קו ה-80% לכל מטבע. המנגנון היה קיים בקוד ולא
הייתה אליו דרך.

מעליה רצועת סקירה: ארבעה אריחים (הכול · עודף · איטי · מת), הון
כלוא לפי ספק בלוח נפרד לכל מטבע — סקאלה נפרדת, בלי המרה ובלי סכום
חוצה מטבעות — ושורה שאומרת ש-209 מתוך 311 אינם באף אחד משלושת
הדליים.

### מה שנבדק ולא היה נעול

פתיחת פריט וסגירתו משאירות את הגלילה, את המיון ואת השורה הראשונה
במקומן. זה עבד קודם; עכשיו יש בדיקה.

---

## 4ג. החלטה שממתינה לך — «מצב טיפול»

המפרט מבקש להפריד את מצב הטיפול (חדש · בבדיקה · ממתין לספק ·
הוזמן · טופל) מהחומרה. **בדקתי, ואי אפשר לבנות את זה בלי שינוי
מודל נתונים.**

היום כל סימון — `campaign`, `treatment`, `ignore`, `handled` —
מוציא את הפריט מתור העבודה ברונג הראשון של `classify` ומעביר אותו
ל-`Q.marked`. כלומר אין מצב «בטיפול ועדיין ברשימה». עמודת «מצב
טיפול» בטבלת החוסרים הייתה מציגה «חדש» בכל שורה, תמיד — מאה
פיקסלים של אפס מידע.

מה שצריך להכריע: האם סימון מסוג «בבדיקה» / «הוזמן» *משאיר* את
הפריט בתור. זו החלטה עסקית ולא עיצובית, ולכן היא לא נלקחה כאן.

---

## 5. מה עוד לא נבנה

* **שינוי רוחב עמודה ושינוי סדר ידני** — לא קיימים. הבורר מוסיף
  ומסיר עמודות בסדר קבוע.
* **תצוגות שמורות** (פריסה + סינון + מיון בשם) — לא קיימות.
* **הסרגל העליון** — בורר מתכנן ומותג, והפרדה בין תאריך ZMRP, תאריך
  ה-ETA ומועד ההעלאה, טרם נבנו.
* **גרפים** (סעיף 5 במפרט) — ROP/SS קיים מול מוצע במסך התכנון, שינוי
  החוסר בין דוחות, עודף לפי ספק — טרם נבנו.
* **מצב טיפול נפרד מחומרה** (סעיף 6) — ראה 4ג: חסום על החלטה עסקית.
* **מסך צר (מובייל)** — נבדק עד 1180px בלבד.

---

## 6. מגבלות ידועות

* במסך 1180px ארבעה שמות תחום נחתכים במסילה. השם המלא מגיע ב-`title`
  ובקורא מסך.
* גופן הטבלה במסלול החוסרים הוא 11.5px גם ב«רגילה» — בחירה קודמת,
  לא תוצר הצפיפות. שווה החלטה נפרדת.
* «מרכז עבודה» סופר `burn + prevent`, והמסך מציג ארבע רשימות שאחת מהן
  היא `burn`. המונה והמסך אינם אותו דבר, וזה מכוון: המונה אומר «כמה
  דורש החלטה», המסך מראה ממה זה מורכב.
